{
  "name": "public-health",
  "version": "0.1.0",
  "private": true,
  "description": "PUBLIC HEALTH — plateforme nationale d'interopérabilité et de services de santé du Burkina Faso (monorepo)",
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "dev": "npm run dev --workspace @public-health/pwa",
    "build": "npm run build --workspace @public-health/pwa",
    "lint": "npm run lint --workspace @public-health/pwa",
    "api:dev": "cd services/api && mvn -B -ntp spring-boot:run",
    "api:test": "cd services/api && mvn -B -ntp verify",
    "verifier": "npm run lint && npm run build && npm run api:test"
  },
  "engines": {
    "node": ">=20.9"
  }
}
