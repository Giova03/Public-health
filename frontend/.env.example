# PUBLIC HEALTH — Frontend (PWA) · variables d'environnement
# Copier en .env.local puis adapter. Ne jamais committer de secret réel.

# Base de l'API backend. VIDE = adaptateur de démonstration intégré
# (routes /api/v1 simulées dans src/app/api, fidèles au contrat).
# Renseigner pour cibler l'API Spring Boot réelle (ex. staging Render) :
# NEXT_PUBLIC_API_BASE_URL=https://public-health-api.onrender.com
NEXT_PUBLIC_API_BASE_URL=
